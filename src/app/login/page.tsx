// This page is no longer needed as Firebase Authentication and login functionality are being removed.
// You can delete this file and its associated route.

export default function LoginPage() {
  return (
    <div className="flex justify-center items-center min-h-[calc(100vh-200px)] py-12">
      <p>Login functionality has been removed as the application is now fully local.</p>
    </div>
  );
}
